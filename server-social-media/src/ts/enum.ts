export enum EnumRole {
  ADMIN = 'admin',
  USER = 'user',
}

export enum EnumTypeMessage {
  STRING = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum EnumRelationShip {
  SINGLE = 0,
  DATING = 1,
  MARRIED = 2,
}

export enum EnumActive {
  INACTIVE = 0,
  ACTIVE = 1,
}
